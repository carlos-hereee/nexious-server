{
  "watch": ["src"],
  "ext": "ts,json,js",
  "ignore": ["node_modules", "dist"],
  "exec": "ts-node -r tsconfig-paths/register ./src/server.ts",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}
